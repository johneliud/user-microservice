name: Ping Service

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping User Microservice
        run: |
          echo "Pinging User Microservice..."
#          response=$(curl -s -o /dev/null -w "%{http_code}" https://${{secrets.BASE_URL}}/api/health)
#          echo "Response: $response"
#          if [ "$response" != "200" ]; then
#            echo "User Microservice is not healthy"
#            exit 1
#          fi